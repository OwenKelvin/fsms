type PaymentModel {
  id: ID!
  name: String!
}

input CreatePaymentInput {
  name: String
}

input UpdatePaymentInput {
  name: String
}

type PaginatedPayment {
  items: [PaymentModel]
  meta: Pagination
}

type CreatePaymentSuccessResponse {
  message: String!
  data: PaymentModel!
}

extend type Query {
  payments(query: QueryParams): PaginatedPayment!
  payment(id: ID!): PaymentModel
}

extend type Mutation {
  updatePayment(
    id: ID!
    params: UpdatePaymentInput
  ): CreatePaymentSuccessResponse

  createPayment(params: CreatePaymentInput): CreatePaymentSuccessResponse

  deletePayment(id: ID!): DeleteSuccessResponse
}
