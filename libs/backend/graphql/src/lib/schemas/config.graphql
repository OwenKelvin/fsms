enum ConfigType {
  EXAM
  EXAM_PAPER
}

type ExamConfig {
  id: ID!
  name: String!
  selected: Boolean
  value: String
}

input ExamConfigInput {
  id: ID!
  selected: Boolean!
  value: String
}

type ConfigModel {
  id: ID!
  name: String!
  type: ConfigType
}

input CreateConfigInput {
  name: String!
  type: ConfigType
}

input UpdateConfigInput {
  name: String!
  type: ConfigType
}

type PaginatedConfig {
  items: [ConfigModel]
  meta: Pagination
}

type CreateConfigSuccessResponse {
  message: String!
  data: ConfigModel!
}

extend type Query {
  configs(query: QueryParams): PaginatedConfig!
  config(id: ID!): ConfigModel
}

extend type Mutation {
  updateConfig(id: ID!, params: UpdateConfigInput): CreateConfigSuccessResponse

  createConfig(params: CreateConfigInput!): CreateConfigSuccessResponse

  deleteConfig(id: ID!): DeleteSuccessResponse
}
