enum ChoiceType {
  CheckBox
  Radio
  Input
}

input QuestionTagInput {
  id: ID
  name: String
}

type QuestionModel {
  id: ID!
  examPaperId: ID!
  exam: ExamModel!
  description: String!
  choiceType: ChoiceType!
  autoMark: Boolean
  correctChoiceExplanation: String
  choices: [ChoiceModel]!
  tags: [TagModel]!
}

input CreateQuestionInput {
  examPaperId: ID!
  description: String!
  choiceType: ChoiceType!
  autoMark: Boolean
  correctChoiceExplanation: String
  choices: [CreateChoiceInput]!
  tags: [QuestionTagInput]!
}

input UpdateQuestionInput {
  examPaperId: ID!
  description: String!
  choiceType: ChoiceType!
  correctChoiceExplanation: String
  autoMark: Boolean
}

type PaginatedQuestion {
  items: [QuestionModel]
  meta: Pagination
}

type CreateQuestionSuccessResponse {
  message: String!
  data: QuestionModel!
}

extend type Query {
  questions(query: QueryParams): PaginatedQuestion!
  question(id: ID!): QuestionModel
}

extend type Mutation {
  updateQuestion(
    id: ID!
    params: UpdateQuestionInput
  ): CreateQuestionSuccessResponse

  createQuestion(params: CreateQuestionInput): CreateQuestionSuccessResponse

  deleteQuestion(id: ID!): DeleteSuccessResponse
}
