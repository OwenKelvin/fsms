extend type Query {
  mpesaTransactionStatus(transactionRef: String!): String
}

extend type Mutation {
  requestMpesaStk(phoneNumber: String!, quoteId: ID!): SuccessResponse
}

type ReceivedPaymentSubscription {
  transaction: TransactionModel
}

extend type Subscription {
  mpesaPaymentReceived(quoteId: ID!): ReceivedPaymentSubscription
}
