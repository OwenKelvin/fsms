extend type Mutation {
  assignRoleToUser(
    userId: ID
    roles: [SelectCategory]
  ): SuccessResponse

  givePermissionsToRole(
    roleId: ID
    permissions: [SelectCategory]
  ): SuccessResponse
}
